info:
  name: List Matches
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/matches?token={{access_token}}&status={{status}}&per_page={{per_page}}&paged={{paged}}"
  params:
    - name: token
      value: "{{access_token}}"
      type: query
    - name: status
      value: "{{status}}"
      type: query
      disabled: true
    - name: per_page
      value: "{{per_page}}"
      type: query
    - name: paged
      value: "{{paged}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
