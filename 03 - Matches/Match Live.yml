info:
  name: Match Live
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/matches/{{mid}}/live?token={{access_token}}"
  params:
    - name: token
      value: "{{access_token}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
