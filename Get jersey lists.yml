info:
  name: Get jersey lists
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:3000/api/v1/cricket/jersey?page=1&limit=10&sortBy=playerName&sortOrder=asc
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
    - name: filter[playerName]
      value: kohli
      type: query
      disabled: true
    - name: filter[format]
      value: ODI
      type: query
      disabled: true
    - name: sortBy
      value: playerName
      type: query
    - name: sortOrder
      value: asc
      type: query
  body:
    type: json
    data: |-
      {
          "playerName": "Virat Kohli",
          "team": "India",
          "format": "T20",
          "isActive":true
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Successful POST request", function () {
            expect(res.getStatus()).to.be.oneOf([200, 201]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: |-
  This is a POST request, submitting data to an API via the request body. This request submits JSON data, and the data is reflected in the response.

  A successful POST request typically returns a `200 OK` or `201 Created` response code.
