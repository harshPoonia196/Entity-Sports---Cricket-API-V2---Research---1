info:
  name: Cricket Exchange - WebSocket Instructions
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Entity Sports changelog confirms a WebSocket instruction page (Apr 28, 2024). This is a push/WebSocket integration, not a normal REST GET.
