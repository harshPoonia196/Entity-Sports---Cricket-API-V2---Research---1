info:
  name: Cricket Stats - Tournament Venue Stats
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/PATH_NOT_VERIFIED"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Confirmed in Entity Sports changelog (May 8, 2024). Exact REST path not publicly retrievable. This placeholder is disabled so the collection never sends a fabricated endpoint.
