info:
  name: Cricket Exchange - Match Ball Event
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/PATH_NOT_VERIFIED"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Confirmed in Entity Sports changelog (Oct 4, 2023). Exact REST path not publicly retrievable. This placeholder is disabled so the collection never sends a fabricated endpoint.
