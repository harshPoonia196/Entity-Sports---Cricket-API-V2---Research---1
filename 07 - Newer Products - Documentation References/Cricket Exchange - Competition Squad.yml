info:
  name: Cricket Exchange - Competition Squad
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/PATH_NOT_VERIFIED"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Confirmed in Entity Sports changelog (Apr 21, 2025). Exact REST path not publicly retrievable. This placeholder is disabled so the collection never sends a fabricated endpoint.
