info:
  name: Cricket Stats - Venue Bowling Stats
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/PATH_NOT_VERIFIED"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Confirmed in Entity Sports changelog (Oct 16, 2025). Exact REST path not publicly retrievable. This placeholder is disabled so the collection never sends a fabricated endpoint.
