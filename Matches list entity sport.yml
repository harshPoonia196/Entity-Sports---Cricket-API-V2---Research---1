info:
  name: Matches list entity sport
  type: http
  seq: 19

http:
  method: GET
  url: https://restapi.entitysport.com/v2/matches/?status=2&token=ec471071441bb2ac538a0ff901abd249
  params:
    - name: filter[playerName]
      value: kohli
      type: query
      disabled: true
    - name: filter[format]
      value: ODI
      type: query
      disabled: true
    - name: status
      value: "2"
      type: query
    - name: token
      value: ec471071441bb2ac538a0ff901abd249
      type: query
  body:
    type: json
    data: |-
      {
          "playerName": "Virat Kohli",
          "team": "India",
          "format": "T20",
          "isActive":true
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Successful POST request", function () {
            expect(res.getStatus()).to.be.oneOf([200, 201]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: |-
  This is a POST request, submitting data to an API via the request body. This request submits JSON data, and the data is reflected in the response.

  A successful POST request typically returns a `200 OK` or `201 Created` response code.
